<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wjjin.refriesh">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Camera" />
        <activity android:name=".Recognize" />
        <activity android:name=".Cookware" />
        <activity android:name=".MenuSearch" />
        <activity android:name=".IngredientSearch" />
        <activity android:name=".AddRecipe" />
        <activity android:name=".RecipeSearch" />
        <activity android:name=".RecipeDetail" />
        <activity android:name=".AdditionalIngredient" />
        <activity android:name=".RecipeByIngredient"></activity>
    </application>

</manifest>